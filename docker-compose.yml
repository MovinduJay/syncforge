services:
  mysql:
    image: mysql:8.0
    container_name: syncforge-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: syncforge_db
    ports:
      - "3306:3306"
    volumes:
      - syncforge_mysql_data:/var/lib/mysql

volumes:
  syncforge_mysql_data: